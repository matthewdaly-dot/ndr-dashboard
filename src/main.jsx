import React from "react"
import ReactDOM from "react-dom/client"
import NDRChart from "./NDRChart.jsx"
ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode><NDRChart /></React.StrictMode>
)